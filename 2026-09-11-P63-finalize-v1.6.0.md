# P63 — Finalisasi & Rilis v1.6.0 "Quality of Life" (Penutup Commit Phase P47–P63)

**Tanggal:** 2026-09-11 · **Fase:** P63 (terakhir) dari roadmap P47–P63 · **Status:** ✅ SELESAI — COMMIT PHASE TUTUP

## 1. Perubahan

- **`updater.py`**: `APP_VERSION` **1.5.0 → 1.6.0** (sinkron dengan `web/package.json` "version": "1.6.0").
- **`README.md`**: badge Release → `v1.6.0 "Quality of Life"`; badge i18n → **3.757 keys**; identitas & tabel status "What is CraftLife?" → v1.6.0; **seksi baru "✨ What's New in v1.6.0"** — tabel 17 item user (P47–P62, 3 fatal ditandai) + checklist verifikasi rilis (expandable), ditempatkan di atas seksi v1.4.0 lama.
- **`UPDATES/2026-09-08-P47-P63-PHASE-SUMMARY.md` (BARU)**: rekap satu commit phase lengkap format 1–8 — 39 file overwrite, 8 file baru, +63 key i18n, 6 migrasi SQLite (tanpa Supabase), kesimpulan per fase, langkah operator rilis.
- **Riwayat versi kini konsisten**: updater 1.6.0 ↔ package.json 1.6.0 ↔ README v1.6.0.

## 2. Verifikasi penuh rilis (semua PASS)

| Uji | Hasil |
|---|---|
| `py_compile` 17 modul inti (MainPyQt6, api_server, database, life_api, studio_api, sync_service, cloud_service, cloud_api, mathtools, music_downloader, translations, updater, web_shell, applog, food_data, holidays, learning_helper) | ✅ |
| `tsc --noEmit` 0 error · `vite build` clean | ✅ |
| **Konsistensi i18n**: `translations.py` (3.757) ↔ `WEB_I18N_KEYS` (semua ada) ↔ `messages.json` id+en (3.757) ↔ salinan `web/public` identik | ✅ |
| **Smoke live S1–S7**: register → bootstrap (55 key) → redeem `WELCOME100` **"+100 XP!"** → redeem ulang ditolak `already_redeemed` → notes add + duplicate tanpa "(copy)" → music lyrics web asli (Tulus, plain+synced, lrclib) → maintenance default 30 hari → versi 1.6.0 sinkron | ✅ |
| Workspace bersih dari artefak uji (db, backups, attachments, driver sementara) | ✅ |

## 3. Langkah operator (di laptop user — untuk rilis publik)

1. Salin seluruh file dari `UPDATES/2026-09-08-P47-P63-PHASE-SUMMARY.md` poin 2 & 3 ke repo.
2. `cd web && npm install && npm run build` → jalankan aplikasi sekali (migrasi SQLite auto, tanpa Supabase).
3. `powershell -File scripts\build.ps1` → zip **tanpa** `.env` & `craftlife.db*` → GitHub Release **`v1.6.0`** (panduan: README → "Windows Build & Release").

## 4. Penutup

Roadmap P47–P63 **selesai 17/17**: 3 bug fatal (redeem #15, chat dobel #4, couple tracking #9) + 14 perbaikan quality-of-life. Total 16 fase terverifikasi (unit + live) + fase finalisasi ini. Commit phase **v1.6.0** resmi ditutup.

---

## Addendum — Revisi README pasca-finalisasi (2026-09-11)

User menandai README masih ber-identitas **v1.4.0** padahal readme harus menggambarkan app terbaru. Revisi (hanya `README.md`, tanpa sentuhan kode/i18n/migrasi):

1. **Identitas → v1.6.0**: "What is CraftLife?" (`**CraftLife v1.6.0** is…`), header tabel status (`Status in v1.6.0`), baris parity (`29 pages, full parity + Craft Design System v2`), instruksi download (`CraftLife-1.6.0`, ukuran zip basi ~371 MB dihapus), project tree (`updater.py 🔄 v1.6.0`), footer (`*CraftLife v1.6.0 — “Quality of Life Release”*`).
2. **Angka i18n**: badge shield (`3%2C757`) + tree `translations.py` (3,757 keys per language) — sebelumnya masih 3,694.
3. **Urutan seksi rilis menurun**: What's New v1.6.0 → UI v2 Craft Design System (v1.5.0) → What's New v1.4.0; anchor `#whats-new` dipindah ke seksi v1.6.0 (target nav "✨ What's New").
4. **Feature Tour disegarkan**: Notes (+attachments image/PDF/txt ≤5 MB, +LaTeX symbol palette), Music (+playlist custom icons, +mini-player yang tetap bermain lintas halaman), Settings (+monthly DB cleanup w/ configurable retention).
5. **Roadmap**: ditambah baris "✅ Quality of Life P47–P63 — COMPLETE (2026-09)" dengan link rekap fase.
6. **Sengaja dipertahankan (historis)**: "Fixed in v1.4.0" (Troubleshooting), checklist i18n 3,694 di dalam seksi What's New v1.4.0, "Tagged on GitHub as CraftLife-1.4.0", frasa updater "since v1.4.0".
